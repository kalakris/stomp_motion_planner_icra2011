#!/bin/bash

if [ -z $1 ]; then
		echo '';
		echo 'usage: callExecuteMRDTrajectory <absolute path + filename>';
		echo '';
		echo 'return: string that states whether the service call was successful.';
		echo '';
else
		rosservice call /execute_mrd_trajectory $1
fi